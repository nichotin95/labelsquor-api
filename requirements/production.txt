# Production requirements
# Optimized for production deployment

-r base.txt

# Production server
gunicorn==21.2.0

# PostgreSQL connection pooling - removed non-existent package
# pgbouncer-python==0.1.0  # This package doesn't exist on PyPI

# APM and monitoring
opentelemetry-api==1.23.0
opentelemetry-sdk==1.23.0
opentelemetry-instrumentation-fastapi==0.44b0

# Security headers
secure==0.3.0

# Request ID tracking
asgi-correlation-id==4.3.1

# These are already included in base.txt:
# slowapi, sentry-sdk, prometheus-fastapi-instrumentator
