# Web crawling requirements
# These are only needed if you're running the crawler component

-r base.txt

# Scraping framework
scrapy==2.13.3
scrapy-playwright==0.0.44
playwright==1.41.1

# HTML parsing (already in base.txt)
# beautifulsoup4==4.13.5
# lxml==6.0.1

# Request handling (already in base.txt)
# requests==2.32.5
